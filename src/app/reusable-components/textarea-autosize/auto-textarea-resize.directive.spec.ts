import { AutoTextareaResizeDirective } from './auto-textarea-resize.directive';

describe('AutoTextareaResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoTextareaResizeDirective();
    expect(directive).toBeTruthy();
  });
});
