{

    "version": "0.2.0",
    "configurations": [

        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "https://app.foresighta.co",
            "webRoot": "${workspaceFolder}"
        }
    ]
}